# 四向穿梭车调度系统 - 用例设计与示意图展示方案

## 一、核心功能点识别

根据项目代码分析，系统包含以下核心功能：

1. **带方向约束的A*路径规划** - 考虑网格方向限制和货物占用
2. **分段路径锁定策略** - 主通道滚动锁定 + 子通道全程锁定
3. **基于负载状态的智能避让** - 负载车优先，空载车避让
4. **动态方向锁定机制** - 负载车在主通道建立方向锁，预防死锁
5. **冲突检测与避让位置优化** - 选择最近可用主通道位置避让
6. **任务分配与可达性验证** - 先验证路径可达性再分配任务

---

## 二、用例场景设计

### 用例1：基础路径规划（展示A*算法与方向约束）

**场景描述**：
- 车辆V1位于(8,7)，空载状态，需要到达货位(3,6)取货
- 网格中存在方向约束：普通通道只能上下移动，主通道可四向移动
- 货位(3,6)位于普通通道，需要从主通道进入

**示意图设计要点**：
1. **初始状态图**：
   - 显示网格地图，标注主通道（蓝色）、普通通道（灰色）、货位（绿色）
   - 标注车辆V1位置（红色圆点）
   - 标注目标位置(3,6)（黄色星号）
   - 在普通通道上标注方向箭头（↑↓），主通道标注四向箭头

2. **路径规划过程图**：
   - 显示A*算法的搜索过程（可选）
   - 用不同颜色标注已探索节点（浅色）和最优路径（深色）

3. **最终路径图**：
   - 用粗线标注规划出的完整路径：(8,7)→(7,7)→(6,7)→(5,7)→(4,7)→(3,7)→(3,6)
   - 标注路径上的每个节点坐标
   - 用不同颜色区分主通道段（蓝色）和普通通道段（绿色）

**展示价值**：
- 展示算法如何考虑方向约束
- 展示路径如何从主通道进入子通道
- 展示空载车辆可以进入任何货位

---

### 用例2：分段路径锁定策略（核心创新点）

**场景描述**：
- 车辆V1规划了从(8,7)到(3,6)的完整路径
- 系统采用分段锁定：主通道锁定前3个格子，子通道锁定全程
- 车辆执行完一段后，释放锁定，继续锁定下一段

**示意图设计要点**：
1. **完整路径图**：
   - 显示完整路径，用不同颜色标注：
     - 主通道段：(8,7)→(7,7)→(6,7)→(5,7)→(4,7)→(3,7) [蓝色]
     - 子通道段：(3,7)→(3,6) [绿色]

2. **第一段锁定图（主通道滚动锁定）**：
   - 锁定前3个主通道格子：(8,7), (7,7), (6,7)
   - 用红色边框标注锁定的格子
   - 标注"锁定范围：前3个主通道格子"
   - 其他主通道格子(5,7), (4,7), (3,7)保持未锁定状态（可用浅色表示）

3. **车辆移动后图**：
   - 车辆移动到(6,7)
   - 释放已通过的格子(8,7), (7,7)的锁定（变为正常颜色）
   - 继续锁定(6,7), (5,7), (4,7)（滚动窗口推进）

4. **进入子通道锁定图**：
   - 车辆到达(3,7)，准备进入子通道
   - 锁定子通道全程：(3,7)→(3,6)
   - 用深绿色标注，标注"子通道全程锁定"
   - 如果目标在深处，还需标注返回路径的锁定

**对比展示**（可选）：
- 传统方法：全路径一次性锁定（所有格子都是红色边框）
- 本方法：分段锁定（只有部分格子是红色边框）
- 用文字说明：主通道利用率提升30-50%

**展示价值**：
- 直观展示核心创新：分段锁定策略
- 展示主通道可以同时容纳多车（因为只锁定部分格子）
- 展示子通道全程锁定的必要性（避免阻塞）

---

### 用例3：基于负载状态的智能避让（核心创新点）

**场景描述**：
- 车辆V1位于(6,7)，空载，空闲状态
- 车辆V2位于(12,7)，负载，刚从货位(10,6)装载货物，需要送往(11,7)
- V2规划路径时发现与V1冲突，触发避让策略

**示意图设计要点**：
1. **冲突检测图**：
   - 显示两车位置：V1(6,7)空载[蓝色]，V2(12,7)负载[红色]
   - 显示V2的规划路径：(12,7)→(11,7)→(10,7)→(9,7)→(8,7)→(7,7)
   - 标注冲突点：路径经过V1所在位置(6,7)附近
   - 用黄色高亮标注冲突区域

2. **避让决策图**：
   - 显示决策逻辑：
     - V2：负载状态（红色标签）
     - V1：空载状态（蓝色标签）
     - 决策结果：V1给V2让路（箭头从V1指向V2）
   - 用文字标注规则："负载车优先，空载车避让"

3. **避让位置选择图**：
   - 显示所有主通道行（假设有y=5, y=7, y=9三行）
   - 标注V1当前位置(6,7)
   - 标注候选避让位置：
     - (6,5) - 距离2，可用（绿色）
     - (6,9) - 距离2，可用（绿色）
     - (6,7) - 当前行，排除（灰色）
   - 标注选择结果：(6,5)或(6,9)，距离最近

4. **避让执行图**：
   - V1的避让路径：(6,7)→(6,6)→(6,5) [虚线，蓝色]
   - V2的原始路径：(12,7)→(11,7)→(10,7)→(9,7)→(8,7)→(7,7) [实线，红色]
   - 标注V1状态：避让中（avoiding）
   - 标注V2状态：等待中（waiting），等待V1完成避让

5. **避让完成图**：
   - V1到达避让位置(6,5)
   - V2从等待状态恢复，继续执行原路径
   - 标注时间线：T1冲突检测 → T2避让执行 → T3避让完成 → T4V2继续

**对比展示**（可选）：
- 传统方法：先到先得，V2等待V1（显示V2长时间等待）
- 本方法：负载优先，V1避让（显示V2快速通过）
- 用数据说明：负载车等待时间减少40-60%

**展示价值**：
- 展示智能避让策略的核心逻辑
- 展示负载车优先的合理性（正在执行高价值任务）
- 展示避让位置选择的优化（最小化避让代价）

---

### 用例4：动态方向锁定机制（核心创新点）

**场景描述**：
- 车辆V1位于(15,7)，负载，需要向左行驶到(8,7)
- 车辆V2位于(3,7)，空载，准备向右行驶到(10,6)取货
- V1先规划路径并建立方向锁定，V2规划时被方向锁阻止

**示意图设计要点**：
1. **V1路径规划图**：
   - 显示V1的完整路径：(15,7)→(14,7)→(13,7)→(12,7)→(11,7)→(10,7)→(9,7)→(8,7)
   - 标注V1状态：负载（红色）
   - 标注主通道段：所有节点都在主通道上

2. **方向锁定建立图**：
   - 在主通道格子上标注方向锁：
     - (15,7), (14,7), (13,7), (12,7), (11,7), (10,7), (9,7), (8,7)
   - 用向左箭头(←)标注方向锁方向
   - 用红色边框标注被锁定的格子
   - 标注说明："负载车V1建立向左方向锁"

3. **V2路径规划尝试图**：
   - 显示V2的规划尝试：(3,7)→(4,7)→(5,7)→(6,7)→(7,7)→(8,7)→(9,7)→(10,7)
   - 标注V2状态：空载（蓝色）
   - 标注冲突点：(8,7), (9,7), (10,7)等被方向锁锁定
   - 用红色X标注无法通过的路径段
   - 标注说明："V2的向右路径被方向锁阻止"

4. **死锁预防效果图**：
   - 显示如果没有方向锁的情况：
     - V1向左，V2向右，在中间位置(8,7)或(9,7)相遇
     - 标注"死锁风险"（红色警告标志）
   - 显示有方向锁的情况：
     - V1顺利通过，V2等待或选择其他路径
     - 标注"死锁已预防"（绿色对勾）

5. **方向锁释放图**：
   - V1完成任务，到达(8,7)
   - 释放所有方向锁（格子恢复为正常颜色）
   - V2可以重新规划路径

**对比展示**（可选）：
- 传统方法：无方向锁，容易死锁（显示两车面对面）
- 本方法：动态方向锁，预防死锁（显示V2被阻止）
- 用数据说明：死锁发生概率降低80%以上

**展示价值**：
- 展示动态方向锁定的工作原理
- 展示死锁预防机制的有效性
- 展示负载车建立方向锁的必要性

---

### 用例5：子通道全程锁定

**场景描述**：
- 车辆V1位于(6,7)，需要到货位(3,6)取货
- 路径包含主通道段和子通道段
- 系统检测到进入子通道，锁定全程（包括返回路径）

**示意图设计要点**：
1. **路径分析图**：
   - 显示完整路径：(6,7)→(5,7)→(4,7)→(3,7)→(3,6)
   - 标注路径段类型：
     - 主通道段：(6,7)→(5,7)→(4,7)→(3,7) [蓝色]
     - 子通道段：(3,7)→(3,6) [绿色]

2. **子通道锁定策略图**：
   - 标注锁定范围：从主通道入口(3,7)到子通道终点(3,6)
   - 如果目标在深处，还需标注返回路径：(3,6)→(3,7)
   - 用深绿色标注锁定的格子
   - 标注说明："子通道全程锁定，避免其他车辆进入造成阻塞"

3. **阻塞预防图**：
   - 显示如果没有全程锁定的情况：
     - V1进入子通道(3,6)
     - 其他车辆V2可能进入(3,7)，阻塞V1返回
     - 标注"阻塞风险"（红色警告）
   - 显示有全程锁定的情况：
     - V1锁定(3,7)和(3,6)
     - V2无法进入(3,7)，等待V1完成
     - 标注"阻塞已预防"（绿色对勾）

**展示价值**：
- 展示子通道全程锁定的必要性
- 展示与主通道滚动锁定的区别
- 展示不同通道类型的差异化策略

---

### 用例6：任务分配与可达性验证

**场景描述**：
- 任务T1：从(8,7)到(3,6)
- 候选车辆：V1位于(10,7)，V2位于(15,5)
- 系统先验证车辆能否到达任务起点，再分配任务

**示意图设计要点**：
1. **任务与车辆位置图**：
   - 显示任务T1起点(8,7)和终点(3,6)
   - 显示V1位置(10,7)和V2位置(15,5)
   - 标注任务信息：任务ID、类型、优先级

2. **可达性验证图**：
   - 对V1：规划从(10,7)到(8,7)的路径
     - 显示路径：(10,7)→(9,7)→(8,7) [绿色，成功]
     - 标注"V1可达，路径长度：2"
   - 对V2：规划从(15,5)到(8,7)的路径
     - 显示路径尝试失败或路径很长 [红色，不优选]
     - 标注"V2路径过长或不可达"

3. **任务分配结果图**：
   - 标注任务分配给V1
   - 显示V1的完整执行路径
   - 标注说明："先验证可达性，避免无效分配"

**对比展示**（可选）：
- 传统方法：就近分配，V2距离更近但可能无法到达（显示分配失败）
- 本方法：可达性验证，V1虽然稍远但可达（显示分配成功）
- 用数据说明：任务分配成功率提升

**展示价值**：
- 展示任务分配前的可达性验证
- 展示避免无效分配的重要性
- 展示智能任务分配策略

---

## 三、示意图设计规范

### 3.1 颜色编码规范

| 元素 | 颜色 | 说明 |
|------|------|------|
| 主通道 | 蓝色 (#4A90E2) | 主干道，可四向移动 |
| 普通通道 | 灰色 (#CCCCCC) | 货位区通道，通常只能上下移动 |
| 接驳口 | 橙色 (#FF9500) | 与输送线连接 |
| 障碍物 | 黑色 (#000000) | 不可通行 |
| 空载车辆 | 蓝色圆点 | 未装载货物 |
| 负载车辆 | 红色圆点 | 已装载货物 |
| 规划路径 | 绿色实线 | 已规划的完整路径 |
| 执行路径段 | 深绿色粗线 | 当前执行的路径段 |
| 锁定格子 | 红色边框 | 被车辆锁定的位置 |
| 方向锁 | 红色箭头 | 主通道的方向锁定 |
| 避让路径 | 蓝色虚线 | 避让车辆的移动路径 |
| 冲突区域 | 黄色高亮 | 路径冲突的位置 |
| 目标位置 | 黄色星号 | 任务起点或终点 |

### 3.2 标注规范

1. **坐标标注**：每个关键位置标注坐标(x,y)
2. **状态标注**：车辆状态用文字标签（idle/pickup/deliver/avoiding/waiting）
3. **路径标注**：用箭头标注移动方向，用数字标注路径顺序
4. **锁定标注**：用边框颜色和文字说明锁定类型
5. **时间标注**：多步骤场景用T1, T2, T3标注时间顺序

### 3.3 布局建议

1. **单场景图**：一个场景一张图，清晰展示关键信息
2. **对比图**：传统方法 vs 本方法并排展示
3. **序列图**：多步骤场景用时间序列展示（T1→T2→T3）
4. **流程图**：算法流程用流程图展示决策逻辑

---

## 四、文章用静态图片制作规范

### 4.1 图片规格要求（针对文章插入）

**尺寸规格**：
- **单栏图片**：宽度 8-10cm（适合单栏排版）
- **双栏图片**：宽度 16-18cm（适合双栏排版）
- **分辨率**：300 DPI（确保打印清晰）
- **宽高比**：建议 4:3 或 16:9（根据内容调整）

**格式要求**：
- **首选格式**：PNG（支持透明背景，适合文章排版）
- **备选格式**：PDF（矢量图，可无损缩放）
- **不推荐**：JPG（有损压缩，不适合线条图）

**文件命名规范**：
- `图1-用例2-分段锁定策略-初始状态.png`
- `图2-用例2-分段锁定策略-滚动推进.png`
- `图3-用例3-智能避让-冲突检测.png`
- 命名格式：`图X-用例Y-功能描述-状态描述.png`

### 4.2 图片设计要点（文章适配）

**1. 简洁清晰原则**：
- 去除不必要的装饰元素
- 关键信息突出显示
- 文字标注清晰可读（建议字号≥10pt）

**2. 黑白兼容性**：
- 考虑文章可能黑白打印
- 除了颜色区分，还要用：
  - 不同线型（实线/虚线/点线）
  - 不同填充图案（斜线/网格/点阵）
  - 不同形状标记（圆点/方块/三角）

**3. 图例说明**：
- 每张图必须包含图例
- 图例位置：右下角或图外
- 图例内容：颜色/线型/标记的含义

**4. 标题和标注**：
- 图片标题：简洁描述图片内容
- 子图标注：如有多张子图，用(a)(b)(c)标注
- 坐标标注：清晰标注关键坐标点

### 4.3 制作工具推荐（文章用图）

**专业工具（推荐）**：
1. **Python matplotlib**：
   - 优点：程序化生成，风格统一，易于修改
   - 适合：基于现有visualizer.py扩展
   - 输出：PNG/PDF，分辨率可控

2. **Adobe Illustrator**：
   - 优点：专业矢量图，高质量输出
   - 适合：精细调整和美化
   - 输出：PDF/PNG，适合印刷

3. **Draw.io / Lucidchart**：
   - 优点：快速制作流程图和示意图
   - 适合：算法流程图、决策树
   - 输出：PNG/SVG

**快速工具**：
- **PowerPoint**：适合快速原型，但需注意分辨率
- **在线工具**：如 Excalidraw（手绘风格，适合示意图）

### 4.4 文章排版建议

**图片位置**：
- 每个用例建议1-2张关键图
- 高优先级用例（2、3、4）可适当增加
- 避免图片过多导致文章冗长

**图片说明**：
- 每张图必须有图题（Figure caption）
- 图题格式：`图X. 功能描述（如：分段路径锁定策略的滚动窗口推进过程）`
- 在正文中引用图片：`如图X所示...`

**图片组合**：
- 对比图：传统方法 vs 本方法并排（适合双栏排版）
- 序列图：T1→T2→T3 时间序列（可用子图(a)(b)(c)）
- 流程图：算法决策流程（适合单栏或跨栏）

---

## 五、用例优先级建议

### 高优先级（必须制作）

1. **用例2：分段路径锁定策略** - 核心创新点，最能体现算法优势
2. **用例3：基于负载状态的智能避让** - 核心创新点，直观展示智能决策
3. **用例4：动态方向锁定机制** - 核心创新点，展示死锁预防

### 中优先级（建议制作）

4. **用例1：基础路径规划** - 展示算法基础能力
5. **用例5：子通道全程锁定** - 展示差异化策略

### 低优先级（可选）

6. **用例6：任务分配与可达性验证** - 可以作为补充说明

---

## 六、文章用图制作建议

### 6.1 制作优先级（针对文章）

**必须制作（核心创新点）**：
1. **用例2：分段路径锁定策略**
   - 图1：完整路径与分段锁定示意图
   - 图2：滚动窗口推进过程（可选，如空间允许）

2. **用例3：基于负载状态的智能避让**
   - 图1：冲突检测与避让决策示意图
   - 图2：避让执行过程（可选）

3. **用例4：动态方向锁定机制**
   - 图1：方向锁定建立与死锁预防示意图

**建议制作（补充说明）**：
4. **用例1：基础路径规划**（如文章需要介绍算法基础）
5. **用例5：子通道全程锁定**（如需要展示差异化策略）

**总计建议**：5-7张图（根据文章长度和版面调整）

### 6.2 每张图的设计要点

**图1：分段路径锁定策略**
- **内容**：展示完整路径、主通道段、子通道段、锁定范围
- **关键信息**：
  - 用不同颜色/线型区分主通道和子通道
  - 用红色边框标注锁定的格子
  - 标注"主通道滚动锁定"和"子通道全程锁定"
- **图题建议**：`图X. 分段路径锁定策略示意图（主通道滚动锁定，子通道全程锁定）`

**图2：智能避让策略**
- **内容**：展示两车位置、冲突区域、避让决策、避让路径
- **关键信息**：
  - 标注车辆负载状态（空载/负载）
  - 标注避让决策规则
  - 用虚线标注避让路径
- **图题建议**：`图X. 基于负载状态的智能避让策略（负载车优先，空载车避让）`

**图3：动态方向锁定**
- **内容**：展示方向锁建立、对向车辆被阻止、死锁预防效果
- **关键信息**：
  - 用箭头标注方向锁方向
  - 标注被阻止的路径（红色X）
  - 对比展示有无方向锁的死锁风险
- **图题建议**：`图X. 动态方向锁定机制预防死锁示意图`

### 6.3 制作步骤建议

**步骤1：确定图片内容**
- 根据文章内容，确定需要哪些图
- 明确每张图要展示的核心信息

**步骤2：设计草图**
- 用纸笔或简单工具绘制草图
- 确定布局、标注位置、图例内容

**步骤3：程序化生成（推荐）**
- 基于现有visualizer.py扩展
- 使用matplotlib绘制，确保风格统一
- 优点：易于修改、批量生成、风格一致

**步骤4：精细调整**
- 调整颜色、线型、标注位置
- 确保黑白打印时也能区分
- 添加图例和标题

**步骤5：导出和优化**
- 导出为PNG（300 DPI）
- 检查文字清晰度
- 压缩文件大小（如需要）

### 6.4 质量检查清单

- [ ] 图片分辨率≥300 DPI
- [ ] 文字清晰可读（字号≥10pt）
- [ ] 颜色编码有图例说明
- [ ] 黑白打印时也能区分（线型/图案）
- [ ] 关键信息不遗漏
- [ ] 图片标题准确描述内容
- [ ] 与文章正文描述一致
- [ ] 文件大小适中（<5MB，如需要）

### 6.5 文章中的图片引用建议

**引用格式示例**：
```
如图1所示，系统采用分段路径锁定策略：主通道采用滚动窗口式锁定，
仅锁定前N个格子（N为步长参数），子通道采用全程锁定...
```

**图片位置建议**：
- 图片紧跟在首次引用的段落之后
- 或集中放在文章末尾（根据期刊要求）
- 确保图片编号与引用一致

---

## 七、文章用图总结

### 7.1 推荐图片清单（文章用）

**核心创新点（必须）**：
1. **图1：分段路径锁定策略示意图**
   - 展示主通道滚动锁定和子通道全程锁定
   - 建议尺寸：单栏或双栏

2. **图2：智能避让策略示意图**
   - 展示负载车优先、空载车避让的决策过程
   - 建议尺寸：单栏或双栏

3. **图3：动态方向锁定机制示意图**
   - 展示方向锁建立和死锁预防效果
   - 建议尺寸：单栏或双栏

**补充说明（可选）**：
4. **图4：基础路径规划示意图**（如需要介绍算法基础）
5. **图5：子通道全程锁定示意图**（如需要展示差异化策略）

**总计**：3-5张图（根据文章长度和版面要求调整）

### 7.2 制作工具推荐（文章用图）

**最佳选择**：Python matplotlib + 现有visualizer.py
- 优点：程序化生成，风格统一，易于修改，分辨率可控
- 输出：PNG（300 DPI）或PDF（矢量图）

**备选方案**：Adobe Illustrator
- 优点：专业矢量图，精细调整
- 适合：需要精细美化的场景

### 7.3 关键注意事项

1. **黑白兼容**：除了颜色，还要用线型、图案区分
2. **清晰度**：文字≥10pt，分辨率≥300 DPI
3. **图例**：每张图必须有图例说明
4. **一致性**：所有图片使用统一的颜色编码和标注规范
5. **准确性**：示意图必须与实际算法逻辑一致

### 7.4 文章中的使用建议

- 图片紧跟在首次引用的段落之后
- 每张图必须有准确的图题
- 在正文中适当引用图片（"如图X所示..."）
- 根据期刊/会议的要求调整图片格式和尺寸
