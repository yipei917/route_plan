version: '3.8'

services:
  route-planner-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: route-planner-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      # 挂载资源目录，方便上传地图文件
      - ./resource:/app/resource:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

